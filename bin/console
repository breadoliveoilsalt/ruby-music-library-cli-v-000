#!/usr/bin/env ruby

load './config/environment.rb'

# load in environment files first -- put require 'irb' right before the irb.start
def reload!
  load ‘./config/environment.rb’
end


require 'irb'

IRB.start
